import HeroSection from "@/components/base/HeroSection";
import { FC } from "react";

const page: FC = () => {
   return (
      <div>
         <HeroSection />
      </div>
   );
};

export default page;
