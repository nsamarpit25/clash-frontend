import { Button } from "@/components/ui/button";
import { FC } from "react";

const page: FC = () => {
   return (
      <div>
         Home Page
         <Button>Hello</Button>
      </div>
   );
};

export default page;
